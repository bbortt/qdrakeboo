schema {
  query: Query
}

extend type Query {
  movie(uuid: String!): Movie
  allMovies(first: Int = 10, after: String): [Movie]
}

type Movie @key(fields: "uuid") {
  uuid: String!
  name: String!
}
