spring:
  application:
    name: edge-gateway
  cloud:
    config:
      enabled: false
    kubernetes:
      config:
        name: edge-gateway
        sources:
          - name: application.yaml
      secrets:
        paths: /etc/qdrakeboo/secret
